/src
  /agents
    intake.ts         # HCS log from SMS intents
    match.ts          # price/logistics suggestion
    escrow.ts         # HTS/HSCS create/release (return-byte)
    delivery.ts       # HCS delivery event
    settlement.ts     # release+receipt
  /hedra
    client.ts         # hedera client factory (testnet)
    hcs.ts            # consensus helpers
    hts.ts            # token helpers
  /nlp
    router.ts         # classify/parse sms → JSON intents (zod validate)
  /sms
    gateway.ts        # stub today; swap to Africa's Talking later
  /web
    server.ts         # express API (webhooks + proof API)
  /data
    buyers.json
    farmers.json
index.ts
.env.example


🧾 Summary
Variable	Description	How to Create
HEDERA_ACCOUNT_ID	Your main orchestrator account	Portal
HEDERA_PRIVATE_KEY	Private key for above	Portal
BUYER_ACCOUNT_ID	Simulated buyer	Portal or SDK
ESCROW_ACCOUNT_ID	Escrow wallet	Portal or SDK
FARMER_ACCOUNT_ID	Simulated farmer	Portal or SDK
HCS_TOPIC_ID	Topic for transaction logs	Portal or SDK
ESCROW_TOKEN_ID	Token ID for payments	Use SDK’s TokenCreateTransaction

How to generate keys 

npx ts-node src/createAccounts.ts

